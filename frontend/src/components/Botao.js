export default function Botao({text}){
    return(<>

        <a class="mt-5 flex w-full justify-center text-xl font-semibold bg-primaryBlue text-white rounded-full border-2 border-accentBlue p-3">
            {text}</a>

    </>)
}
